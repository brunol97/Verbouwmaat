/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Enable if you use React Server Components with AI streaming
    // dynamicIO: true,
  },
};

module.exports = nextConfig;
